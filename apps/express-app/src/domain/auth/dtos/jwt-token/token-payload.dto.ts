/**
 * Represents the payload structure for authentication tokens.
 *
 * @property userId - The unique identifier of the user associated with the token.
 */
export interface ITokenPayloadDto {
  userId: string;
}
